[package]
name = "game-of-life"
version = "0.2.2"
edition = "2026"

[dependencies]
crossterm = { version = "^0.29.0", features = ["event-stream"] }
rand = "^0.10.0"
termion = "*"
tokio = { version = "^1", features = ["full"] }
futures = "^0.3"
futures-timer = "^3.0"

[profile.release]
debug = true
